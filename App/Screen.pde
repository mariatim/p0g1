void showScreen1(){
  /**
  Start screen
  **/
  background(192, 64, 0);

}

void showScreen2(){
  /**
  Menu screen
  **/
  
}

void showScreen3(int elementNumber){
  /**
  Element screen
  **/
  
}

void showScreen4(){
  /**
  End Screen
  **/
  
}

/*
int circleX = 100;
int circleY = 100;
int rad = 100;

PImage background1;

// Setup block
void setup()
{
  size(841, 1189);
  ellipseMode(RADIUS);
  background1 = loadImage("backImage.jpg");  
  stroke(0);
  noFill();
}

// Draw block
void draw()
{

  // Adding the background
  image (background1, 0, 0);
  background1.resize(width, height);

  // Setting up the buttons
  // Button 1
  fill(#FF7E7F);
  ellipse(circleX, circleY, rad, rad);

  if (mousePressed)
  {
    if ((mouseX - circleX < rad) && (mouseY - circleY < rad) && (mouseX - circleX > -rad) && (mouseY - circleY > -rad))
    {
      fill(#FF2418);
      println("Button 1");
    }
  }
  // Button 2
  fill(#FF7E7D);
  ellipse(circleX+300, circleY+100, rad, rad);

  if (mousePressed)
  {
    if ((mouseX - circleX < rad) && (mouseY - circleY < rad) && (mouseX - circleX > -rad) && (mouseY - circleY > -rad))
    {
      fill(#FF2471);
      println("Button 2");
    }
  }
    
  // Button 3  
  fill(#FF7E7E);
  ellipse(circleX+100, circleY+300, rad, rad);
  
  if (mousePressed)
  {
   if ((mouseX - circleX < rad) && (mouseY - circleY < rad) && (mouseX - circleX > -rad) && (mouseY - circleY > -rad))
   {
     fill(#FF2478);
     println("Button 3");
   }
  }
  // Button 4
  fill(#FF7E7E);
  ellipse(circleX+300, circleY+100, rad, rad);
     
  if (mousePressed)
   {
    if ((mouseX - circleX < rad) && (mouseY - circleY < rad) && (mouseX - circleX > -rad) && (mouseY - circleY > -rad))
    {
      fill(#FF2478);
      println("Button 4");
    }
   }
  }
  */
